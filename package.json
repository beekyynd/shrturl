{

  "scripts": {
    "dev": "nodemon serverside/main.js",
    "build": "npm install && npm install --prefix clientside && npm run build --prefix clientside",
    "start": "NODE_ENV=production node serverside/main.js"
  },
  "type": "module",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.7.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
